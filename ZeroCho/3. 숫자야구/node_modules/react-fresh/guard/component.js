"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var component_1 = require("../router/component");
var react_1 = require("react");
var PureGuard = /** @class */ (function (_super) {
    __extends(PureGuard, _super);
    function PureGuard() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    PureGuard.prototype.render = function () {
        var _a = this.props, rule = _a.rule, children = _a.children;
        var ruleValidator = PureGuard.guards[rule];
        // Guard rule does not exist
        if (!ruleValidator)
            throw new Error('Guard does not exist');
        // Guard returned false
        if (!ruleValidator()) {
            component_1.PureRouter.redirect('403');
            return null;
        }
        return children;
    };
    PureGuard.guards = {};
    PureGuard.addGuard = function (name, rule) {
        if (PureGuard.guards[name])
            throw new Error('Guard is already registered');
        PureGuard.guards[name] = rule;
    };
    PureGuard.reset = function () {
        PureGuard.guards = {};
    };
    return PureGuard;
}(react_1.PureComponent));
exports.PureGuard = PureGuard;
//# sourceMappingURL=component.js.map